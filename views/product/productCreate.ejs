<!DOCTYPE html>
<html>
    <head>
        <%- include("../partials/head-base", {title: "Detalle del producto"}) %>
        <link rel="stylesheet" href="/css/productCreate.css" />
        <link rel="stylesheet" href="/css/productCreate.css" />
    </head>
    <body class="bg-muted">
        <header><%- include("../partials/header") %></header>
        <main>
            <h1>Cargá los datos del producto que querés vender</h1>
            <form
                action="/product"
                method="POST"
                class="create-product-form padding-10"
            >
                <div class="display-flex flex-direction-column padding-5">
                    <label for="name">Nombre</label>
                    <input type="text" id="name" name="name" />
                </div>
                <div class="display-flex flex-direction-column padding-5">
                    <label for="price">Precio</label>
                    <input type="number" id="price" name="price" />
                </div>
                <div class="display-flex flex-direction-column padding-5">
                    <label for="description">Descripción</label>
                    <textarea
                        name="description"
                        id="description"
                        cols="30"
                        rows="10"
                    ></textarea>
                </div>
            </form>
        </main>
        <footer class="bg-dark text-light text-align-center">
            <%- include("../partials/footer") %>
        </footer>
    </body>
</html>
