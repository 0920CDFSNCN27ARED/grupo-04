<!DOCTYPE html>
<html>
    <head>
       <%- include("partials/head-base", {title: "Login"}) %>
    <body>
        <header><%- include("partials/header-simple") %></header>
    </body>
        <form action="/auth/login" method="POST"></form>
        <div class="login-box">
            <!-- <img src="/images/isologo2.png" class="avatar" alt="Avatar Image" /> -->
            <h1>Login</h1>
            <form action="/auth/login" method="POST">
                <!-- USERNAME INPUT -->
                <label for="email">Ingresá tu correo electrónico</label>
                <!-- cambiar a tipo email pero corregir CSS porque se rompe el estilo-->
                <input
                    type="text"
                    name="email"
                    id="email"
                    placeholder="Correo Electrónico"
                    required
                />
                <!-- PASSWORD INPUT -->
                <label for="password">Ingresá tu contraseña</label>
                <input
                    type="password"
                    name="password"
                    id="password"
                    placeholder="Ingresá tu contraseña"
                    required
                />
                <label for="rememberMe">Recordame</label>
                <input type="checkbox" name="rememberMe" id="rememberMe">
                <input type="submit" value="Iniciar Sesión" />
                <a href="#">¿Olvidaste tu contraseña?</a><br />
                <a href="/auth/register">¿Aún no tenes cuenta?</a>
            </form>
        </div>

</html>
